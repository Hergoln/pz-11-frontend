import React from 'react';
import { Canvas } from '@react-three/fiber';

function AgarntPage() {

    return (
        <Canvas>
            <ambientLight />
        </Canvas>
    );
};

export default AgarntPage;