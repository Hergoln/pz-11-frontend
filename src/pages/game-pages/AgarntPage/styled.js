import styled from 'styled-components';
import Box from '@mui/material/Box';

const ScoreDisplay = styled(Box)`
    font-weight: bold;
    font-size: 24px;
    position: absolute;
    display: block;
    width: 100%;
    top: 10px;
`;

export { ScoreDisplay };
